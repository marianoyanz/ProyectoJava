import java.sql.Connection;
import java.sql.SQLException;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) throws SQLException {

        /*

        //Conexion con la base de datos

        managerDB conexion_db = new managerDB();

        Connection c = conexion_db.connect_to_db();

        //Busqueda de un cliente

        int id; Scanner lector = new Scanner(System.in);

        System.out.println("Ingrese el id del cliente: ");
        id = lector.nextInt();

        users customer = conexion_db.buscar_cliente(c, id);

        if(customer != null)  System.out.printf(customer.getName());

        c.close();

        */
    }

}

